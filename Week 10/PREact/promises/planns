callback/promise pairs
	chosen to show the reasons why

links to resources

a few exercises


resolve and reject act as return statements

can't so much be passed arguments as they need to be used with context smartness
- yup.  wrapping new in a function with variables passed in

only one arg

use wrappers, call em in thens

all for async, then for sync

